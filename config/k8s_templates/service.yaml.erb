apiVersion: v1
kind: Service
metadata:
  labels:
    run: <%= service.slug %>
  name: <%= service.slug %>
  namespace: <%= @environment.namespace %>
spec:
  ports:
  - name: http
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    run: <%= service.slug %>
